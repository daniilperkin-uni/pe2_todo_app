# Copy this file to .env and adjust the values for your environment.
# The docker-compose setup reads DB_PASSWORD; the Spring Boot application
# reads DB_USERNAME, DB_PASSWORD and DB_URL (see api/src/main/resources/application.properties).
DB_USERNAME=root
DB_PASSWORD=changeme
DB_URL=jdbc:mariadb://localhost:3306/todoapp
